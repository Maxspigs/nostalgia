import { AppBar, Container, Toolbar, Typography } from '@mui/material';

export function Home() {

    return (
        <Container maxWidth="xl">
            <Typography>Bienvenue sur Nostalgia, un serveur UO qui est basée sur Zyx, Teilia et Terra. Venez joindre l'aventure.</Typography>
        </Container>
    );
}
